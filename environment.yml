name: gapo_env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - pandas=1.3.5
  - numpy=1.21.2
  - tqdm=4.62.2
  - cpuonly # Especifica a versão para CPU. Veja a nota abaixo para GPU.
  - pip
  - pip:
    - fair-esm
